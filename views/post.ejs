
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= post.title %> | Blog</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-tr from-blue-50 to-purple-100 min-h-screen flex flex-col items-center px-4 py-10">

  <div class="bg-white shadow-xl rounded-xl max-w-3xl w-full p-8 space-y-6">
 
    <h1 class="text-4xl font-bold text-gray-800 text-center"><%= post.title %></h1>

    
    <p class="text-lg text-gray-700 leading-relaxed whitespace-pre-line">
      <%= post.content %>
    </p>

     
    <div class="flex justify-center space-x-4 pt-6">
    
      <a href="/edit/<%= index %> " class="px-6 py-2 bg-yellow-400 hover:bg-yellow-500 text-white rounded-md font-medium transition duration-200 shadow">
        ✏️ Edit
      </a>

   
      <form action="/delete/<%= index %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
        <button type="submit"onsubmit="return confirm('Are you sure you want to delete this post?');" class="px-6 py-2 bg-red-500 hover:bg-red-600 text-white rounded-md font-medium transition duration-200 shadow">
          🗑️ Delete
        </button>
      </form>
    </div>
  </div>

 
  <div class="mt-8">
    <a href="/" class="text-blue-600 hover:underline text-lg">← Back to Home</a>
  </div>

</body>
</html>

