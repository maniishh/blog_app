<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Compose</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Tailwind CSS -->
  <link href="https://unpkg.com/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <style>
    body {
      font-family: 'sans-serif';
    }
  </style>
</head>
<body class="m-0 p-0 overflow-hidden text-white relative">

  <!-- ✅ Background Image -->
  <!-- <img src="images/comp.jpg" alt="Background" class="absolute inset-0 w-full h-full object-cover z-0" /> -->

  <!-- 🔵 Foreground Content with backdrop blur and form -->
  <div id="vanta-compose-bg" class="relative z-10 min-h-screen flex items-center justify-center px-4">
    <div class="bg-white/0 border border-white/20 backdrop-blur-sm shadow-lg rounded-xl p-8 max-w-xl w-full text-black">
      <h1 class="text-3xl font-bold text-center mb-6">📝 Create a New Blog Post</h1>

      <form action="/compose" method="POST" enctype="multipart/form-data" class="space-y-4">
        <!-- 📝 Title -->
        <input 
          type="text" 
          name="title" 
          placeholder="Post Title" 
          required
          class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-300 text-black bg-transparent placeholder-gray-500"
        >

        <!-- 📌 Content -->
        <textarea 
          name="content" 
          placeholder="Write your content here..." 
          required
          rows="8"
          class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-300 text-black bg-transparent placeholder-gray-500 resize-none"
        ></textarea>

        <!-- 📷 Image Upload -->
        <input 
          type="file" 
          name="image" 
          accept="image/*"
          class="w-full text-gray-700 file:bg-blue-600 file:text-white file:px-4 file:py-2 file:rounded-md file:border-none border border-gray-300 rounded-md bg-transparent placeholder-gray-500"
        >

        <!-- 🔘 Submit Button -->
        <button 
          type="submit" 
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-md transition duration-200"
        >
          📤 Publish Post
        </button>
      </form>
    </div>
  </div>

</body>
</html>


